{
  "name": "root",
  "private": true,
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-plugin-prettier": "^3.4.1",
    "lerna": "^4.0.0",
    "prettier": "^2.3.2"
  },
  "scripts": {
    "test": "jest",
    "lint": "eslint --ext .js packages/**/*.js --no-error-on-unmatched-pattern --fix"
  }
}
